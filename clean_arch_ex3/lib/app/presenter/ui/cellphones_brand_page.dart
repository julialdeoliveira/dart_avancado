import 'package:flutter/material.dart';

class CellphonesBrandPage extends StatelessWidget {

  const CellphonesBrandPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}